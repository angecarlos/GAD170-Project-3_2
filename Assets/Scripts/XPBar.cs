using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class XPBar : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //say how much XP the player has
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
